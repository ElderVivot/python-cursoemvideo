import math
angulo = float(input('Digite o valor do ângulo: '))
cosseno = math.cos(math.radians(angulo))
seno = math.sin(math.radians(angulo))
tang = math.tan(math.radians(angulo))
print(angulo, cosseno, seno, tang)
